import { createSlice } from "@reduxjs/toolkit";

const RandomReducerSlice = createSlice({
  name: "Random reducer",
  initialState: {},
  reducers: {},
});

export default RandomReducerSlice.reducer;
